using NUnit.Framework;

namespace SqlToGraphite.UnitTests
{
    // ReSharper disable InconsistentNaming
    [TestFixture]
    public class With_genericSerializer
    {
        [Test]
        public void Should_deserialise_successfully()
        {
            //SqlToGraphiteConfig sqlToGraphiteConfig = new SqlToGraphiteConfig();            
            //TextReader input = new StringReader(Resource1.TestData_goodconfig);
            //XmlReader reader = new XmlTextReader(input);
            //sqlToGraphiteConfig.ReadXml(reader);
            //Assert.That(sqlToGraphiteConfig.Clients.Count, Is.EqualTo(2));
            //Assert.That(sqlToGraphiteConfig.Jobs.Count, Is.EqualTo(1));
            //Assert.That(sqlToGraphiteConfig.Hosts.Count, Is.EqualTo(1));
            //Assert.That(sqlToGraphiteConfig.Templates.Count, Is.EqualTo(1));
            //Assert.That(sqlToGraphiteConfig.Templates[0].WorkItems[0], Is.TypeOf<SqlToGraphite.Plugin.Wmi.WmiClient>());
        }
    }
}