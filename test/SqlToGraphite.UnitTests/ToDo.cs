using NUnit.Framework;

namespace SqlToGraphite.UnitTests
{
    // ReSharper disable InconsistentNaming
    [TestFixture]
    public class ToDo
    {
        [Test]
        public void Should_()
        {
            Assert.Fail("need to remove unused classes");
            Assert.Fail("ImplementOracleClient");
            Assert.Fail("configuration validation (client types are known)");
        }
    }
}